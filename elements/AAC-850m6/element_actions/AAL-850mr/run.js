function(instance, properties, context) {
	if (window.psb4phS3) {
        const animate = properties["Animate"];
        window.b6p3gpD3(window.psb4phS3, undefined === animate ? true : animate);
    }
}